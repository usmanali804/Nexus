The Nexus project is built using React.
It follows a component-based structure.

Pages folder contains main screens like:
- Login
- Dashboard
- Profile

Components folder contains reusable UI parts like:
- Navbar
- Sidebar
- Cards

Routing is handled using React Router.
User data and login state are handled using state and local storage.